enum Boxes {
  products,
  sales,
}

enum Debounces {
  FILTER,
  LOADING,
  PICK_DATE,
}

enum Images {
  logo,
  background,
}

enum Prefs {
  ACCESS_TOKEN,
  REFRESH_TOKEN,
}

enum Routes {
  dashboard,
  inquiry,
  inventory,
  landing,
}

enum StatusTypes {
  ERROR,
  INFO,
  SUCCESS,
  WARN,
}
