enum Boxes {
  deductions,
  products,
  returns,
  sales,
}

enum Debounces {
  FILTER,
  LOADING,
  PICK_DATE,
}

enum Images {
  logo,
  background,
}

enum Prefs {
  ACCESS_TOKEN,
  REFRESH_TOKEN,
}

enum Routes {
  dashboard,
  deductions,
  inquiry,
  inventory,
  landing,
  returns,
}

enum StatusTypes {
  ERROR,
  INFO,
  SUCCESS,
  WARN,
}
