enum Boxes {
  products,
  sales,
}

enum Debounces {
  FILTER,
  LOADING,
}

enum Images {
  logo,
}

enum Prefs {
  ACCESS_TOKEN,
  REFRESH_TOKEN,
}

enum Routes {
  dashboard,
  inventory,
  landing,
}

enum StatusTypes {
  ERROR,
  INFO,
  SUCCESS,
  WARN,
}
