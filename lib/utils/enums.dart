enum Debounces {
  LOADING,
}

enum Images {
  logo,
  logotext,
}

enum Prefs {
  ACCESS_TOKEN,
  REFRESH_TOKEN,
}

enum Routes {
  createpassword,
  home,
  landing,
  submitotp,
  registration,
  requestotp,
}

enum StatusTypes {
  ERROR,
  INFO,
  SUCCESS,
  WARN,
}
